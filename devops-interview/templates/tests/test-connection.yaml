apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-interview.fullname" . }}-test-connection"
  labels:
    {{- include "devops-interview.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-interview.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
